An applicaton which displays  files and folder of existing path provided
User has to login first to access the files and folder if user doesn't exist in the database(MongoDB for database) User has to create their account then user can access the files and folder available
(Mini File Explorer Project);
